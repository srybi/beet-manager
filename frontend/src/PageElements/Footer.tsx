import styled from "styled-components";

const FooterWrapper = styled.footer`
  grid-column: 1 / 3;
  background: #b4b7b2;
`;

export const Footer = () => {
    return (
        <>
        <FooterWrapper></FooterWrapper>
        </>
    );
};
